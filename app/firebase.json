{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "hosting": {
    "public": "dist",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [{ "source": "**", "destination": "/index.html" }],
    "headers": [
      {
        "//": "index.html names which JS and CSS to load, so caching it pins a visitor to whatever build they first saw. Firebase's default is an hour, which is why a deploy could take that long to reach a phone. Revalidate every time: it is one small conditional request, usually answered with a 304.",
        "source": "/index.html",
        "headers": [
          { "key": "Cache-Control", "value": "no-cache, must-revalidate" }
        ]
      },
      {
        "//": "Everything under /assets carries a content hash in its filename, so a changed file is a different URL. Those can be cached forever.",
        "source": "/assets/**",
        "headers": [
          { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
        ]
      },
      {
        "//": "The manifest and icon change rarely but must not be pinned for a year.",
        "source": "/@(manifest.webmanifest|icon.svg)",
        "headers": [
          { "key": "Cache-Control", "value": "public, max-age=3600" }
        ]
      },
      {
        "//": "How a tab that is already open finds out a newer build shipped. Caching this would defeat the entire point of it.",
        "source": "/version.json",
        "headers": [
          { "key": "Cache-Control", "value": "no-cache, must-revalidate" }
        ]
      }
    ]
  },
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "ui": { "enabled": true, "port": 4000 },
    "singleProjectMode": true
  }
}
